# 제약사 공지 통합 HTML 플러그인 설치·사용설명서

배포 범위: 사내 사용자 전용  
관리 담당: `beodnamu_ITteam`  
플러그인 이름: `pharma-notice-html`

## 1. 플러그인 소개

제약사에서 전달한 품절·입고, 수수료, 의약품 정보 공지 이미지를 구조화하고 사용자의 검토와 승인을 거쳐 HTML 및 PDF 공지를 생성하도록 지원합니다.

이 플러그인은 이미지에 보이는 값과 사용자가 직접 입력한 수정값만 사용합니다. 이미지에 없는 제품명, 성분, 보험코드, 날짜 등을 임의로 추정하지 않습니다.

## 2. 준비 사항

설치 전에 다음 사항을 확인하세요.

- Windows PC에 Codex가 설치되어 있어야 합니다.
- 플러그인 ZIP 파일을 `beodnamu_ITteam` 또는 승인된 사내 공유 경로에서 받아야 합니다.
- PowerShell을 실행할 수 있어야 합니다.
- 설치 폴더를 임의로 이동하거나 이름을 변경하지 않는 것이 좋습니다.

GitHub 계정은 필요하지 않습니다.

## 3. ZIP 파일 받는 방법

다음 중 회사에서 허용한 방법으로 ZIP 파일을 받습니다.

- 사내 파일 공유 서버
- 사내 메신저의 파일 전송 기능
- 담당자가 전달한 USB 또는 이동식 저장장치
- 사내 이메일 첨부파일

파일명 예시:

```text
pharma-plugin-marketplace.zip
```

외부 메신저나 개인 클라우드로 재전송하지 마세요.

### 파일 무결성 확인

담당자가 SHA-256 값을 함께 제공한 경우 PowerShell에서 다음 명령으로 확인할 수 있습니다.

```powershell
Get-FileHash "C:\다운로드경로\pharma-plugin-marketplace.zip" -Algorithm SHA256
```

표시된 해시가 담당자가 안내한 값과 다르면 설치하지 말고 `beodnamu_ITteam`에 문의하세요.

## 4. 압축 해제

ZIP 파일 안에서 직접 실행하지 말고 반드시 전체 압축을 해제합니다.

권장 설치 경로:

```text
C:\beodnamu\codex-plugins\pharma-plugin-marketplace
```

### Windows 탐색기로 압축 해제

1. ZIP 파일을 마우스 오른쪽 버튼으로 클릭합니다.
2. `압축 풀기` 또는 `모두 추출`을 선택합니다.
3. 대상 폴더를 다음과 같이 지정합니다.

```text
C:\beodnamu\codex-plugins
```

4. 압축 해제 후 다음 파일 구조인지 확인합니다.

```text
pharma-plugin-marketplace
├─ .agents
│  └─ plugins
│     └─ marketplace.json
├─ plugins
│  └─ pharma-notice-html
├─ README.md
├─ START-HERE.txt
└─ 직원용_설치_및_사용설명서.md
```

`.agents`는 숨김 폴더일 수 있습니다. 이 폴더가 빠지면 설치되지 않습니다.

## 5. 최초 설치

PowerShell을 열고 다음 두 명령을 순서대로 실행합니다.

```powershell
codex plugin marketplace add "C:\beodnamu\codex-plugins\pharma-plugin-marketplace"
codex plugin add pharma-notice-html@pharma-team
```

설치 상태를 확인합니다.

```powershell
codex plugin list
```

목록에 `pharma-notice-html`이 표시되면 설치가 완료된 것입니다.

설치 후 기존 Codex 작업을 계속 사용하지 말고 새로운 작업을 열어야 합니다.

## 6. 기본 사용 순서

### 6.1 플러그인 시작

새 Codex 작업에서 다음 문구를 입력합니다.

```text
@pharma-notice-html 제약사 공지 이미지를 올릴게요. 업로드 완료 후 분석해 주세요.
```

### 6.2 공지 이미지 업로드

공지 이미지가 여러 장이면 순서대로 모두 올립니다. 한 공고가 여러 장으로 나뉘어 있어도 괜찮습니다.

업로드 중에는 분석을 요청하지 말고 모든 이미지 전송이 끝난 뒤 다음과 같이 입력합니다.

```text
업로드 완료
```

### 6.3 분석 결과 확인

플러그인이 다음 내용을 표시합니다.

- 업로드한 이미지 수
- 판독한 공고 수
- 재업로드가 필요한 이미지
- 품절 및 입고 항목
- 수수료 항목
- 의약품 정보 항목

성분명이나 날짜가 이미지에 없으면 임의로 채우지 않고 `공고 이미지 미표기`, `확인 중`, `미정` 등으로 표시합니다.

### 6.4 값 수정

표시된 확인 목록에서 등호 오른쪽 값만 수정해 다시 보냅니다.

예시:

```text
제품명 10mg = 제약회사명 | 성분명
```

잘못 판독된 항목을 제외하려면 다음처럼 요청합니다.

```text
제품명 10mg 항목은 제외해 주세요.
```

수정 후에는 전체 목록이 다시 표시되므로 누락 여부를 한 번 더 확인합니다.

### 6.5 승인

모든 값이 맞는지 확인한 뒤 다음 중 하나를 입력합니다.

```text
그대로
수정 없음
이대로
바로 생성
```

승인하기 전에는 HTML과 PDF가 생성되지 않습니다.

### 6.6 결과 파일 받기

생성이 완료되면 다음 두 링크가 제공됩니다.

- HTML 다운로드
- PDF 다운로드

두 파일을 모두 열어 회사명, 제품명, 성분명, 날짜, 규격, 수수료율과 표 페이지 나눔을 원본 공지와 대조하세요.

## 7. 이미지 업로드 권장사항

- 표 전체가 잘리지 않도록 촬영합니다.
- 작은 글자가 확대해도 읽히는 해상도를 사용합니다.
- 같은 이미지를 여러 번 올리지 않습니다.
- 여러 장이면 원본 페이지 순서대로 올립니다.
- 카카오톡 등에서 이미지가 압축되어 흐려졌다면 원본 파일로 다시 받습니다.
- 개인정보나 업무상 불필요한 내용은 업로드 전에 가립니다.

## 8. 업데이트 설치

`beodnamu_ITteam`에서 새 ZIP을 전달한 경우 다음 순서로 업데이트합니다.

1. Codex를 종료합니다.
2. 기존 폴더를 백업용으로 이름 변경합니다.

```text
C:\beodnamu\codex-plugins\pharma-plugin-marketplace-backup
```

3. 새 ZIP을 기존과 동일한 경로에 압축 해제합니다.

```text
C:\beodnamu\codex-plugins\pharma-plugin-marketplace
```

4. PowerShell에서 재설치합니다.

```powershell
codex plugin add pharma-notice-html@pharma-team
```

5. Codex에서 새로운 작업을 열어 테스트합니다.
6. 정상 동작을 확인한 뒤 백업 폴더를 회사 정책에 따라 정리합니다.

마켓플레이스 폴더 경로가 바뀐 경우에만 다음 명령을 다시 실행합니다.

```powershell
codex plugin marketplace add "변경된-마켓플레이스-폴더의-절대경로"
```

## 9. 문제 해결

### `codex` 명령을 찾을 수 없다고 표시되는 경우

- Codex 설치 여부를 확인합니다.
- Codex 설치 직후라면 PowerShell을 닫고 다시 엽니다.
- 계속 실패하면 오류 화면을 캡처해 `beodnamu_ITteam`에 전달합니다.

### 마켓플레이스를 찾을 수 없는 경우

다음 파일이 실제로 존재하는지 확인합니다.

```text
C:\beodnamu\codex-plugins\pharma-plugin-marketplace\.agents\plugins\marketplace.json
```

ZIP 안쪽에 폴더가 한 번 더 중첩되어 있지 않은지도 확인합니다.

잘못된 예:

```text
pharma-plugin-marketplace\pharma-plugin-marketplace\.agents
```

### 플러그인이 목록에 보이지 않는 경우

다음 명령을 다시 실행합니다.

```powershell
codex plugin marketplace add "C:\beodnamu\codex-plugins\pharma-plugin-marketplace"
codex plugin add pharma-notice-html@pharma-team
codex plugin list
```

그런 다음 Codex에서 새 작업을 엽니다.

### 이미지가 판독되지 않는 경우

- 원본 이미지를 다시 업로드합니다.
- 잘린 표가 없도록 전체 페이지를 올립니다.
- 재업로드해도 실패하면 플러그인이 안내하는 수기 입력 양식에 따라 값을 입력합니다.

### HTML 또는 PDF가 생성되지 않는 경우

- 수정 확인 목록을 모두 검토했는지 확인합니다.
- `그대로` 또는 `이대로`처럼 명시적인 승인 문구를 입력합니다.
- 재업로드 필요 이미지나 해결되지 않은 충돌이 남아 있으면 먼저 처리합니다.

## 10. 보안 및 사용 제한

- 사내 업무 목적으로만 사용합니다.
- ZIP, 플러그인 파일, HTML 디자인과 로고를 외부에 전달하지 않습니다.
- 공지 이미지와 결과 파일은 회사의 문서 보관 규정을 따릅니다.
- 생성 결과를 검수하지 않은 상태로 외부에 배포하지 않습니다.
- 플러그인 파일을 임의로 수정한 경우 공식 지원 대상에서 제외될 수 있습니다.

## 11. 문의 시 전달할 정보

문제가 발생하면 다음 정보를 `beodnamu_ITteam`에 전달하세요.

- 오류가 발생한 단계
- 입력한 명령 또는 요청 문구
- 오류 메시지 전체
- 오류 화면 캡처
- 사용한 ZIP 파일명 또는 버전
- 재현 가능한지 여부

공지 이미지에 민감정보가 포함되어 있다면 회사가 승인한 경로로만 전달하세요.
